# Lead Demo

Simple proof-of-concept app that:

1. reads a matching email from Gmail
2. parses lead fields from the email body
3. creates a lead in Close CRM

## Requirements

- Node.js
- Gmail API OAuth credentials
- Close OAuth app credentials

## Setup

1. Install dependencies

```bash
npm install